1. README.md 文档优化。 突出自动审查、自动维护 等内容。
2. 测试与 pencil、paper的互动，来制作原型图，然后根据原型图输出UI
3. 审查的时候，注入 tasks.json 路径，和当前任务。
4. 文档规则，例如：ts类型声明。（应该只需要一句话了，不需要给示例）
5. 增加 tasks.json 的自我维护. 确认由AI维护的文件
6. 文件缓存要去掉， tasks.json、template 等文件的的缓存。 不然读取不到更新的内容？
7. ``` [INFO]  
    [INFO]  检测到 session_result 写入，5 分钟内模型未终止将自动中断
    ```
8. run 的过程，还会去加载 requirement.md 是有问题的。
9. tasks.json 错误，不应该终端，应该尝试修复。 同时进行维护精简。
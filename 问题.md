# 输出字符限制 不能是 || 80

# session_result 硬截断也要确保格式正确

# .mcp.json 需要 clauder-code auth *** 之后才生成，感觉有点问题

# 变更了模式， .mcp.json 应该也要有变动，而不是不动

# setup 模式应该重构下，全量重设，就一个个开始配置。 可以 setup 部分配置，相当于 config指令。这样，可以单独配置设置中断时间，单独配置playwright mcp模式。 你觉得呢

# 如何识别或者加入 "进程被锁" 杀线程的提示。b

# templates/CLAUDE.md 优化

# 在点击生成按钮，然后使用 browser_wait_for 等待 PPT 预览出现  timeout=300s）：  很好的解决了瞎猜不等待的问题。